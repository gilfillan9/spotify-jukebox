<div class="view-details">
    <div class="view-detail-top">
        <img src="{{images.[0].url}}" class="detail-image"/>

        <div class="detail-text">
            <h3>Artist</h3>

            <h2>{{name}}</h2>
        </div>
    </div>
    <h4>Popular Tracks</h4>

    <div class="track-table-wrap">
        <table class="track-table" data-uri="{{uri}}">
            {{> trackTableTop}}
            {{#each tracks}}
                {{> track}}
            {{/each}}
        </table>
    </div>
    <h4>Albums</h4>
    <ul class="pills">
        {{#each albums}}
            {{> albumPill}}
        {{/each}}
    </ul>
</div>