<li data-uri="{{uri}}" data-action="view">
    <img src="{{images.[0].url}}"/>

    <div class="text-content">
        <span class="name">{{name}}</span>
        <span class="artist">{{tracks.total}} songs</span>
    </div>
</li>