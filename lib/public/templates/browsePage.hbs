<div class="card">
    <h2>{{message}}</h2>

    <div class="pills featured">
        {{#each featured}}
            {{> playlistPill}}
        {{/each}}
    </div>
</div>
<div class="card">
    <h2>Categories</h2>

    <div class="pills">
        {{#each categories}}
            {{> categoryPill}}
        {{/each}}
    </div>
</div>