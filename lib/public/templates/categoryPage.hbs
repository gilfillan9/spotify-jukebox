<div class="view-details">
    <div class="view-detail-top">
        <img src="{{icons.[0].url}}" class="detail-image"/>

        <div class="detail-text">
            <h4>Category</h4>

            <h3>{{name}}</h3>
        </div>
    </div>
    <div class="browse">
        <ul class="pills">
            {{#each playlists}}
                {{> playlistPill}}
            {{/each}}
        </ul>
    </div>
</div>